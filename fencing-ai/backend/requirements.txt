fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
anthropic==0.34.2
mediapipe==0.10.14
opencv-python-headless==4.10.0.84
# yt-dlp must stay current — YouTube changes break older versions regularly.
# Floor (>=) rather than pinned so fresh installs pull the latest extractor fixes.
# If downloads start failing, run:  pip install -U yt-dlp   (or: yt-dlp -U)
yt-dlp>=2026.3.17
ffmpeg-python==0.2.0
imageio-ffmpeg==0.5.1   # bundles a static ffmpeg binary — no system install needed
numpy==1.26.4
Pillow==10.4.0
aiofiles==24.1.0
python-dotenv==1.0.1
httpx==0.27.2
# Training (Step 3 — pose-sequence action classifier)
torch==2.4.1
