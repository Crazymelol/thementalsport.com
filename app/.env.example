# Copy to .env (git-ignored) and fill in — Expo bakes EXPO_PUBLIC_* into the app.
# COACH_URL: your deployed mina URL (no trailing slash), e.g. https://mina-xxxx.vercel.app
# COACH_TOKEN: must equal the COACH_APP_TOKEN env var set in Vercel.
EXPO_PUBLIC_COACH_URL=
EXPO_PUBLIC_COACH_TOKEN=
